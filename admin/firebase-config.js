(function () {
    'use strict';

    /*
     * Firebase Web configuration is intentionally unset until the verified
     * Firebase project is registered. Firebase Web config is public project
     * metadata; never place passwords, administrator UIDs, or service secrets
     * in this file.
     */
    window.FLYING_INTELLIGENCE_FIREBASE = Object.freeze({
        config: null,
        functionsRegion: 'us-central1',
        emulators: null
    });
}());
